# Discord Bot Agent Guidance Bridge

`../AGENTS.md` is the canonical project contract.

Before modifying this repository:

1. read `../AGENTS.md`;
2. read `skills/maintain-architecture/SKILL.md`;
3. inspect `../arch.md` and the affected execution path;
4. preserve existing working-tree changes and run the verification required by the
   canonical contract.

Use `skills/verify-realtime-data/SKILL.md` before relying on changeable external
model, API, limit, price, or source-availability facts.
